import React from 'react';

const Verify =()=>{
   // document.getElementById("fetch").innerHTML = "Would you like to update your question?"

//console.log(element);


return (
   <div>
<div id= "verify">"Wait while we fetch your response"</div>
<div id="fetch">Loading...</div>
</div>);
}
export default Verify;